import { FC } from "react";

const ErrorMessage: FC = () => {
  return (
    <h3>
      There was an error during fetching balance, please refresh the page and
      try again
    </h3>
  );
};

export default ErrorMessage;
